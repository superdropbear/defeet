# Anime reference images

Place approved anime reference images in this directory. JPEG, PNG, and WebP
files are included in `images/bot-images.json` by the manifest builder and join
the combined image pool used by `/defeet`.

Files in `images/ai` remain a separate collection and are not served.
